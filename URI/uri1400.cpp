#include<iostream>
#include<vector>

using namespace std;



int main(){
    int pe, m, k, num = 1, index = 1;
    

    cin >> pe >> m >> k; // m é a pessoa q vai bater a palma e k na hora da palma

    while(num < 1000000){// cada while é uma palma
        
        if(pe == 0 && m == 0 && k == 0){
            break;
        }

        
        
        index++;
        n++;
    }
    return 0;
}